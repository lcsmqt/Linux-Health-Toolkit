# Contribuindo

1. Crie um fork e uma branch descritiva.
2. Instale as dependências: `pip install -r requirements.txt`
3. Rode `ruff check src tests` e `pytest`.
4. Abra um pull request com descrição do problema e da solução.

Não envie secrets, dumps de `/etc/shadow` nem logs reais de produção.
